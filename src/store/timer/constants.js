export const SET_TIMER_START = `SET_TIMER_START`;

export const SET_TIMER_END = `SET_TIMER_END`;

export const CLEAR_TIMER = `CLEAR_TIMER`;

export const CREATE_TIMER = `CREATE_TIMER`;

export const TIMER_ACTIONS = new Set([
  SET_TIMER_START,
  CREATE_TIMER,
  SET_TIMER_END,
  CLEAR_TIMER,
]);
